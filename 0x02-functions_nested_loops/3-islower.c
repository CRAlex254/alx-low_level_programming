#include "main.h"

/**
 * _islower(int c): function that checks for lowercase character
 *
 * Return: 1
 * Return: o if otherwise
 */

int _islower(int c);
{
	int c;

	if (c is lowercase)
		return = 1;
	else 
		return = 0;
}
